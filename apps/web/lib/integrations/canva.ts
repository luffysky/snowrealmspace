/**
 * Canva OAuth + PKCE cookie 助手。實作已移至 @snowrealm/design-sync（單一來源）。
 * 此檔保留匯入路徑 `@/lib/integrations/canva` 不變（admin 轉換器與 connect/callback 路由沿用），
 * 僅轉出 package。
 */
export * from '@snowrealm/design-sync'
